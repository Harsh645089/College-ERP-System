# Quick Run Instructions

If the IDE run configuration doesn't work, use the batch file:

1. Open a terminal in the ERP directory
2. Run: `run.bat`

This will:
- Clean old class files
- Compile all Java files
- Run the application with the correct classpath

The application will start with the login window.

